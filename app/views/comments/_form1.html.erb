<%= form_for([@challenge, @challenge.comments.build]) do |form| %>
  <div class='container'>
          <div class="form-group">
            <%= form.rich_text_area :content, placeholder: "Comment*", class: 'form-control', style: "width: 100%; height: 125px;" %>
          </div>         
          <div class="form-group">
            <input type="submit" name="btnSubmit" class="btn btn-primary" value="Post Comment"/>
          </div>
  </div>
<% end %>
